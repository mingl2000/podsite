# Contracts — Podcast site

This static site has no runtime API. All data is embedded in the repository under `data/episodes.json`.

If you need a machine-readable contract for the episodes data, use the provided JSON Schema in `episodes.schema.json`.
